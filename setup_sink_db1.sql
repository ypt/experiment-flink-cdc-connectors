CREATE TABLE users_full_name_and_favorite_color (
  id BIGSERIAL PRIMARY KEY,
  full_name VARCHAR,
  favorite_color VARCHAR
);

CREATE TABLE users_full_name_and_favorite_color_2 (
  id BIGSERIAL PRIMARY KEY,
  full_name VARCHAR,
  favorite_color VARCHAR
);
